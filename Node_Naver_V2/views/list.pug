extends layout

block content
    style.
        #quarterbox{
            overflow: auto;
            height : 500px;
        }
        .text-bold{
            font-weight: bold;
        }
    div.w3-container.w3-center.w3-margin

        h1= "도서 검색 서비스"

        form(action="/book/naver", method="POST")
            input(type="text", name="search", placeholder="검색할 도서명 입력").w3-input
    each book,index in books
        div#page_content.w3-main.w3-content.w3-center
            div.w3-col-padding.w3-center
                div#quarterbox.w3-quarter
                    img(src=book.image)
                    h4
                        a(href=book.link) !{book.title}
                    p#author 저자 : !{book.author}  &#40;!{book.publisher} &#41;
                    p#price &#8361;!{book.price}
                    p#pubdate 출판년월일 : !{book.pubdate}
                    p#isbn ISBN : !{book.isbn}
                    details
                        summery
                            p#description !{book.description}